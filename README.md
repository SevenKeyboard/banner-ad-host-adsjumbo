# banner-ad-host-adsjumbo
Displays an AdsJumbo banner window for a calling application.

To use it with your own AdsJumbo account, copy `AdsJumboConfig.example.cs` to `AdsJumboConfig.cs` and fill in your application IDs and trusted certificate thumbprints.